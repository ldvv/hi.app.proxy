<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sys.gateway.dao.ICustomerDao" >

    <insert id="updateToDB" parameterType="com.sys.gateway.vo.CustomerDB" >
        insert into customer1 (id, baseInfo, labels, orderInfo, otherInfo, time)
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.id,jdbcType=VARCHAR}, #{item.baseInfo,jdbcType=VARCHAR}, #{item.labels,jdbcType=VARCHAR},
            #{item.orderInfo,jdbcType=VARCHAR}, #{item.otherInfo,jdbcType=VARCHAR}, #{item.time,jdbcType=VARCHAR})
        </foreach>
    </insert>
</mapper>